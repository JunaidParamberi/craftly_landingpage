{
  "name": "craftly-landingpage",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . -p 3000",
    "start": "npx serve . -p 3000",
    "build": "echo 'Static site — no build step required'"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
